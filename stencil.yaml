###########################################################
#                                                         #
#            game_stencil config yaml                     #
#                                                         #
###########################################################
version: 0.1

game_stencil:
    .git
    .gitignore
    .cocos-project.json
    CMakeLists.txt
    clean.sh
    README.md
    LICENCE
    AUTHOR
    TODOLIST
    WORKNOTES

    bin:
        # compiled binary packages

    Classes:
        AppDelegate.h
        AppDelegate.cpp
        HelloWorldScene.h
        HelloWorldScene.cpp

    cocos2d:
        # cocos2d-x system library directory
        # DO NOT COMMIT TO GIT

    common:
        # C-language common library
        byteorder.h
        dhlist.h

    config:
        # all config files

    doc:
        # documents

    ecs:
        model:
            component:
                interface:
            core:
            config:
            entity:
            interface:
            system:

    experimental:
        # test cases

    proj.win32:
    proj.android:

    Resources:
        # cocos public resources for all

    scripts:
        # python or shell scripts

    thirdparty:
        # third party sources or tools
        mxml-2.9:
        crash3:
